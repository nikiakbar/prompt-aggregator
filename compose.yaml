services:
  prompt-aggregator:
    build: .
    ports:
      - "4000:4000"
    volumes:
      # host folder ./data -> container /input (Input source for images)
      - ./data:/input
      # host folder ./output -> container /data (Destination for exported wildcard.txt)
      - ./output:/data
    restart: unless-stopped
